#include <stdio.h>
#include <string.h>

/**
 * _memcpy - a function that copies memory area
 * @n: number of bytes
 * @src: copied bytes from memory area
 * @dest: where memory is stored
 *  
 * Return: byte changed 
 */

char *_memcpy(char *dest, char *src, unsigned int n)

{

	unsigned int i; 
	
	for (i = 0; i => n ; r++)
	{
	dest[i] = src;
	}
	return (dest);
}
